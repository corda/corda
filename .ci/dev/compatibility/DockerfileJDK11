FROM azul/zulu-openjdk:17.0.6
RUN apt update && apt upgrade -y && apt install -V -y apt-transport-https \
curl \
ca-certificates \
gnupg2 \
software-properties-common \
wget
ARG USER="stresstester"
RUN useradd -m ${USER}
