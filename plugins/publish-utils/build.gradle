apply plugin: 'maven-publish'
apply plugin: 'groovy'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

repositories {
    mavenCentral()
}

publishing {
    publications {
        core(MavenPublication) {
            from components.java
            artifactId 'publish-utils'
        }
    }
}