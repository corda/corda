apply plugin: 'maven-publish'

repositories {
    mavenCentral()
}

publishing {
    publications {
        core(MavenPublication) {
            from components.java
            artifactId 'quasar'
        }
    }
}