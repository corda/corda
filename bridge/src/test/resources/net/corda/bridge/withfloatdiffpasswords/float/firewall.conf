firewallMode = FloatOuter
inboundConfig : {
    listeningAddress = "0.0.0.0:10005"
    customSSLConfiguration : {
        keyStorePassword = "floatinboundkeypassword"
        keyStorePrivateKeyPassword = "floatinboundprivatekeypassword"
        trustStorePassword = "floatinboundtrustpassword"
        sslKeystore = "float/inboundcerts/outboundkeys.jks"
        trustStoreFile = "float/inboundcerts/outboundtrust.jks"
        revocationConfig {
            mode = SOFT_FAIL
        }
    }
}
floatOuterConfig : {
    floatAddress = "localhost:12005"
    expectedCertificateSubject = "O=Bank A, L=London, C=GB"
    tunnelSSLConfiguration : {
        keyStorePassword = "bridgetunnelkeypassword"
        keyStorePrivateKeyPassword = "bridgetunnelprivatekeypassword"
        trustStorePassword = "bridgetunneltrustpassword"
        sslKeystore = "bridge/tunnelcerts/tunnelkeys.jks"
        trustStoreFile = "bridge/tunnelcerts/tunneltrust.jks"
        revocationConfig {
            mode = SOFT_FAIL
        }
    }
}
networkParametersPath = network-parameters