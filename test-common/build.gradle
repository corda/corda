apply plugin: 'kotlin'
apply plugin: 'net.corda.plugins.publish-utils'

dependencies {
    compile project(':client:rpc')
}

jar {
    baseName 'corda-test-common'
}

publish {
    name = jar.baseName
}
