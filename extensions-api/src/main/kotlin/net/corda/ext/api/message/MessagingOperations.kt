package net.corda.ext.api.message

import net.corda.core.CordaInternal

/**
 * Exposes messaging API which will allow services to produce and consume Artemis messages
 */
@CordaInternal
interface MessagingOperations {
    // Left empty for now but interface content will be defined at a later point
}